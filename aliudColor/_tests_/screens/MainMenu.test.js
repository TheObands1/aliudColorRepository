import React from 'react';
import renderer from 'react-test-renderer';
import MockedMainMenuNavigator from './MockedMainMenuNavigator';
import MainMenu from "../../screens/MainMenu/index"

describe('tests for the <MainMenu/>', () => {
    /*
    it('Render the component correctly ', () => {
        const tree = renderer.create(<MockedMainMenuNavigator component = {MainMenu}/>);
        expect(tree).toMatchSnapshot();
    });
    */
  });